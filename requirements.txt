# LineEdge Python dependencies.
#
# Hand-curated direct dependencies only, UTF-8, pinned. Transitive packages are
# deliberately absent: pip resolves them, and listing them here turns every
# upstream release into a merge conflict. If you need a byte-identical
# environment, generate a lock file from this list rather than editing it.

# --- Core service -----------------------------------------------------------
pydantic==2.12.4            # app/models/schemas: odds payload validation
pydantic-settings==2.11.0   # app/core/config: LINEEDGE_* env-driven Settings
SQLAlchemy==2.0.44          # app/storage: Mapped[] declarative ORM over SQLite and MySQL
pymysql>=1.1.0              # MySQL DBAPI driver
cryptography>=42.0.0        # MySQL caching_sha2_password authentication
RapidFuzz==3.14.3           # app/engine/resolver: token_sort_ratio entity matching

# --- Dashboard --------------------------------------------------------------
streamlit==1.51.0           # app/ui/dashboard.py
pandas==2.3.2               # dashboard dataframes; notebook analysis
plotly==6.5.0               # dashboard charts

# --- Tests ------------------------------------------------------------------
pytest==8.4.2               # tests/

# --- Docs, notebook and asset generation (optional) -------------------------
# Not needed to run the service; required by scripts/ and notebooks/demo.ipynb.
matplotlib==3.10.6          # scripts/render_architecture.py, notebook figures
numpy==2.3.3                # notebooks/demo.ipynb
playwright==1.60.0          # scripts/capture_docs.py (also needs: playwright install chromium)
nbformat==5.10.4            # re-executing notebooks/demo.ipynb
nbclient==0.10.2            # re-executing notebooks/demo.ipynb
