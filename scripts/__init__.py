"""Developer utility scripts (market simulation, demo seeding, doc capture).

Declared as a real package so `from scripts.market_sim import ...` resolves to
this directory rather than an unrelated `scripts` namespace package that may
exist elsewhere on sys.path (pywin32 ships one).
"""
